# تغییرات

## 1.1.11 - 2026-07-31

- پیمایش با کلیدهای بالا و پایین دیگر کل فهرست را دوباره چاپ نمی‌کند؛ فقط خط
  انتخاب قبلی، خط انتخاب جدید و Prompt به‌روزرسانی می‌شوند تا پرپرزدن منو در
  Termius موبایل حذف شود.
- صفحه‌های موقت مانند وضعیت تونل، خروجی Health Check و فهرست بکاپ پس از Enter
  درجا پاک می‌شوند و دیگر با بازگشت به منوی اصلی در Scrollback دیده نمی‌شوند.
- تاریخچه واقعی ترمینال مربوط به قبل از اجرای `bh` همچنان حفظ می‌شود و هیچ
  دستور `CSI 3 J` یا پاک‌سازی مخرب Scrollback استفاده نشده است.
- تست PTY رگرسیون اکنون کنترل می‌کند گزینه‌های بدون تغییر هنگام Arrow Navigation
  دوباره چاپ نشوند و خروجی صفحات موقت وارد تاریخچه ترمینال نشود.

## 1.1.10 - 2026-07-31

- لوگوی کامل پنج‌خطی `HOMA` در ترمینال‌های موبایل باریک بازگردانده شد؛ عرض کم
  دیگر باعث حذف هویت بصری پروژه نمی‌شود.
- حالت تک‌خطی فقط برای ترمینال‌های واقعاً کوتاه‌تر از ۲۰ ردیف استفاده می‌شود و
  همان حالت نیز نشان `[ HOMA ]` را حفظ می‌کند.
- تست PTY اکنون وجود تمام خطوط لوگو را در ابعاد ۲۰×۳۶، ۲۴×۴۰ و ۳۲×۳۶ کنترل
  می‌کند تا این رگرسیون در نسخه‌های بعدی تکرار نشود.

## 1.1.9 - 2026-07-30

- رندر منو برای ترمینال‌های کوتاه و باریک بازطراحی شد؛ هدر در کمتر از ۲۲ ردیف
  تک‌خطی است و برچسب گزینه‌ها پیش از Wrap شدن متناسب با عرض واقعی کوتاه می‌شوند.
- تست PTY به ابعاد ۱۲×۸۰، ۱۶×۴۸، ۲۰×۳۶، ۲۴×۴۰ و ۳۲×۳۶ گسترش یافت و خروج،
  پیمایش با جهت‌ها و پاک‌سازی Scrollback را کنترل می‌کند.
- Update هسته پس از Restart چند بررسی متوالی Active انجام می‌دهد؛ Crash با تأخیر
  دیگر موفق تلقی نمی‌شود و باینری قبلی همراه با وضعیت سرویس‌ها Rollback می‌شود.
- دریافت `--latest` از Release رسمی و بررسی SHA-256 منتشرشده برای Asset اضافه شد؛
  نبود Digest به‌صورت پیش‌فرض خطاست و عبور فقط با گزینه صریح انجام می‌شود.
- Parser بدون وابستگی برای Digest اضافه شد تا سیستم فاقد Python و jq نیز بتواند
  Metadata رسمی Release را بررسی کند.
- Restore امن و تراکنشی بکاپ به CLI و منو اضافه شد؛ Safety Backup، جلوگیری از
  Path Traversal و Symlink، اعتبارسنجی Unit/Config و Rollback شکست را پوشش می‌دهد.
- آرشیوهای Release و Backup پیش از استخراج از نظر مسیر مطلق/Traversal، نوع Entry،
  تعداد فایل و سقف حجم بازشده اعتبارسنجی می‌شوند؛ Metadata اضافی، Unit ناموجود و
  کانفیگ بدون مرجع نیز در Restore رد می‌شود.
- فرمت بکاپ به نسخه ۳ ارتقا یافت و مسیر اصلی هر کانفیگ و وضعیت Active/Enabled هر
  Unit را نگه می‌دارد؛ بازیابی آرشیوهای قدیمی نسخه ۱ و ۲ نیز حفظ شده است.
- پورت‌های دارای صفر ابتدایی صریحاً رد می‌شوند تا تفسیر Octal در Bash رخ ندهد.
- Installer گزینه‌های `--latest` و `--allow-unverified-download` را پشتیبانی و
  ترکیب‌های متناقض را پیش از تغییر فایل‌ها رد می‌کند.
- ترتیب Test Runner اصلاح شد و تست‌های Backup Restore، Binary Rollback و Release
  Update فقط یک‌بار و پس از Syntax Check اجرا می‌شوند.
- راهنمای کامل انگلیسی، استقرار انگلیسی و عیب‌یابی انگلیسی اضافه شد.

## 1.1.8 - 2026-07-30

- فاصله بزرگ و مصنوعی بین خروجی قبل از `bh` و خط فرمان پس از خروج حذف شد؛
  فقط ردیف‌های واقعاً استفاده‌شده با گزارش موقعیت مکان‌نما به Scrollback
  منتقل می‌شوند، نه یک صفحه کامل از خطوط خالی.
- خروجی واقعی عملیات پس از فشردن Enter بدون افزودن فضای خالی به تاریخچه منتقل
  می‌شود و فریم‌های رابط منو همچنان از Scrollback دور می‌مانند.
- چیدمان تعاملی در ترمینال‌های کوتاه فشرده شد و دو ردیف حاشیه امن دارد تا
  هیچ خطی از لوگوی HOMA حتی با اختلاف ابعاد Termius وارد تاریخچه نشود.
- تست PTY اکنون موقعیت واقعی مکان‌نما را پاسخ می‌دهد و دقیقاً سناریوی
  «خروجی‌های قبل از bh → اجرای منو → خروج و بازگشت Prompt» را بدون فاصله
  خالی یا بقایای لوگو شبیه‌سازی می‌کند.

## 1.1.7 - 2026-07-30

- رگرسیون باقی‌ماندن هدر و اطلاعات سرور در Scrollback پس از انتخاب گزینه یا
  بازگشت از زیرمنو برطرف شد. فریم جاری منو اکنون پیش از هر انتقال، بدون چاپ
  Line Feed اضافه درجا پاک می‌شود.
- دو Line Feed تکراری پس از انتخاب گزینه‌های منوی اصلی و مدیریت تونل حذف شدند؛
  در ترمینال ۲۴ ردیفی دیگر هیچ خطی از لوگوی `HOMA` به تاریخچه رانده نمی‌شود.
- پاک‌سازی پایان انتخاب روی `stderr` انجام می‌شود تا انتخاب تونل در Command
  Substitution آلوده نشود و نام سرویس دقیق باقی بماند.
- Scrollback مربوط به قبل از اجرای `bh` همچنان حفظ می‌شود و خروجی واقعی
  عملیات مدیریتی نیز قابل مرور است؛ فقط فریم‌های رابط منو وارد تاریخچه
  نمی‌شوند.

## 1.1.6 - 2026-07-30

- اجرای منو از Alternate Screen به بافر عادی ترمینال منتقل شد تا Scrollback
  اصلی هنگام بازبودن `bh` در Termius با لمس و کشیدن صفحه قابل مشاهده باشد.
- پیش از رسم منو، نمای فعلی با Line Feed استاندارد به Scrollback منتقل می‌شود؛
  بنابراین دستورها و خروجی‌های قبل از `bh` حفظ می‌شوند.
- تمام مسیرهای `CSI 3 J` که سابقه ترمینال را پاک می‌کردند حذف شدند؛ تعویض صفحه
  و خروج فقط نمای جاری را با `CSI 2 J` پاک می‌کند.
- برای ترمینال‌های موبایل بلند، دو ردیف امن بالای هدر در نظر گرفته شد تا هر پنج
  خط لوگوی ASCII «HOMA» زیر نوار Termius کامل نمایش داده شود؛ ترمینال‌های کوتاه
  به‌صورت خودکار چیدمان فشرده را دریافت می‌کنند.
- تست PTY اکنون حفظ خروجی قبل از `bh`، ممنوع‌بودن Alternate Screen و
  پاک‌سازی Scrollback، کامل‌بودن پنج خط لوگو، پاک‌شدن نمای منو در خروج و همه
  مسیرهای پیمایش با جهت‌ها را کنترل می‌کند.

## 1.1.5 - 2026-07-30

- پیمایش واقعی با کلیدهای `Up` و `Down` و انتخاب با `Enter` به منوی اصلی،
  تمام زیرمنوها و فهرست انتخاب تونل اضافه شد؛ ورود مستقیم شماره گزینه نیز حفظ
  شده است.
- ورودی انتخاب در PTY به‌صورت پیوسته بدون Echo خوانده می‌شود تا Escape
  Sequenceهای کلیدهای جهت به شکل `^[[A` و `^[[B` روی صفحه چاپ نشوند.
- وضعیت Echo و Canonical ترمینال در خروج عادی، `Esc`، EOF، `Ctrl+C`،
  `SIGTERM` و قطع ناگهانی منو بازیابی می‌شود.
- تست PTY برای تمام منوهای سطح اول، زیرمنوهای تو‌در‌تو، انتخاب چندتونلی،
  دو نوع رایج Escape Sequence، ورود عددی و چرخه Alternate Screen اضافه شد.
- نصب Manager تراکنشی و فایل‌به‌فایل اتمیک شد؛ شکست میانی، همه فایل‌های نسخه
  قبلی را بازمی‌گرداند و باینری، کانفیگ، Unit و PID تونل را تغییر نمی‌دهد.
- جایگزینی کانفیگ و Unit و همچنین تغییر Mapping با Rename اتمیک انجام می‌شود.
- افزودن Mapping دارای پورت یکسان با پورت کنترل، در کانفیگ‌های جدید و قدیمی
  پیش از تغییر فایل رد می‌شود.
- آرشیو بکاپ پیش از انتشار با `tar` بازخوانی و اعتبارسنجی می‌شود.
- Uninstaller مسیرهای گسترده یا خطرناک مانند `/` را پیش از هر حذف رد می‌کند.
- تمام تست‌های Syntax، TOML، systemd Unit، Rollback، ارتقا، منو، بکاپ،
  Health Check و Uninstall و همچنین ShellCheck بدون هشدار اجرا شدند.

## 1.1.4 - 2026-07-29

- منوی تعاملی داخل Alternate Screen استاندارد ترمینال اجرا می‌شود و هنگام خروج
  عادی، قطع ارتباط یا دریافت `SIGTERM` بافر قبلی را دقیقاً بازیابی می‌کند؛
  بنابراین هیچ منو یا لوگوی تکراری در Termius باقی نمی‌ماند.
- تمام صفحات و زیرمنوها پیش از رسم مجدد پاک می‌شوند و گزینه Back بدون درخواست
  Enter اضافه به منوی قبلی برمی‌گردد.
- شکست یک فرمان مدیریتی دیگر کل منوی تعاملی را ناگهانی نمی‌بندد و EOF نیز خروج
  تمیز دارد.
- خروجی خالی Listening Port و Active Connection در منو و Doctor با پیام روشن
  نمایش داده می‌شود.
- استقرار و جایگزینی Server/Client تراکنشی شد؛ در صورت شکست Restart، کانفیگ،
  Unit و وضعیت قبلی سرویس به‌صورت خودکار Rollback می‌شوند.
- آرگومان‌های ناقص CLI و Installer با پیام دقیق رد می‌شوند؛ IPv4 نامعتبر،
  نگاشت تکراری و تداخل پورت عمومی با پورت کنترل نیز پیش از تغییر فایل‌ها متوقف
  می‌شوند.
- افزودن و حذف Mapping برای کانفیگ‌های قدیمی با آرایه تک‌خطی `ports = [...]`
  نیز پشتیبانی می‌شود.
- نصب اتمیک دیگر Permission پوشه‌های موجودی مانند `/etc/systemd/system` و
  `/etc/cron.d` را تغییر نمی‌دهد.
- نام بکاپ‌ها و آرشیوها با دقت نانوثانیه تولید می‌شود تا اجرای سریع و پشت‌سرهم
  باعث بازنویسی فایل قبلی نشود.
- حذف کامل، قبل از پاک‌کردن فایل‌ها Unitها، کانفیگ و هسته را کپی و صحت نسخه
  پشتیبان را بررسی می‌کند و در صورت توقف‌نشدن سرویس عملیات را متوقف می‌سازد.
- تست جامع تمام منوها، PTY واقعی، تک‌تونل و چندتونل، ورودی‌های نامعتبر،
  Rollback، نصب ارتقایی، Health Check، Mapping قدیمی، بکاپ و Uninstall اضافه
  شد؛ کل سورس نیز بدون هشدار ShellCheck بررسی می‌شود.

## 1.1.3 - 2026-07-29

- هر منو با پاک‌سازی صفحه و Scrollback بازطراحی می‌شود تا خروجی منوهای قبلی باقی نماند.
- هنگام خروج، صفحه و سابقه قابل‌مشاهده منو پاک می‌شوند.
- در سرور تک‌تونل، همان سرویس بدون درخواست شماره اضافه انتخاب می‌شود.
- خروجی خالی پورت‌های Listening در Client با توضیح روشن نمایش داده می‌شود.

## 1.1.2 - 2026-07-29

- تمام منوها، پیام‌های تأیید، خطاها و خروجی‌های مدیریتی به انگلیسی تبدیل شدند.
- لوگوی ASCII و عنوان `HOMA` فقط یک‌بار هنگام ورود به منوی اصلی نمایش داده می‌شوند.
- برگشت از زیرمنوها فقط فهرست گزینه‌های اصلی را بازنمایش می‌دهد و هدر را تکرار نمی‌کند.
- تست خودکار برای انگلیسی‌بودن منو و جلوگیری از تکرار هدر اضافه شد.
- نصب ارتقایی همچنان باینری، کانفیگ، سرویس و PID تونل فعال را تغییر نمی‌دهد.

## 1.1.1 - 2026-07-29

- لوگوی ASCII بالای منو از Backhaul به HOMA تغییر کرد.
- تمام منوها، گزینه‌ها و ورودی‌های تعاملی به فینگلیش چپ‌چین تبدیل شدند.
- سازگاری نمایش در Termius و ترمینال‌هایی که متن راست‌به‌چپ را جابه‌جا می‌کنند.
- نصب ارتقایی همچنان باینری، کانفیگ، سرویس و PID تونل فعال را تغییر نمی‌دهد.

## 1.1.0 - 2026-07-29

- منوی رنگی فارسی با ساختار مدیریت چندتونلی
- فرمان کوتاه `bh`
- شناسایی سرویس‌های موجود و قدیمی بدون بازنویسی کانفیگ
- Health Check شرطی و Cron دارای قفل `flock`
- حفظ خودکار باینری موجود هنگام نصب Manager
- افزودن و حذف نگاشت پورت همراه با بکاپ و Rollback
- بکاپ کانفیگ‌ها، سرویس‌ها و Cron
- آرشیو قابل‌بازیابی به‌جای حذف مستقیم تونل
- پشتیبانی از IPv6 با قالب `[IPv6]:PORT`
- حذف امن Manager بدون توقف تونل‌ها
- ابزارهای تشخیص شبکه بدون دستکاری خودکار `sysctl`
- تست نصب ارتقایی، سرویس‌های Legacy و ترمیم Health Check

## 1.0.0 - 2026-07-29

- نصب خودکار Backhaul روی Ubuntu و Debian
- پشتیبانی از نقش Server و Client
- مدیریت چند تونل مستقل با سرویس‌های جداگانه systemd
- پشتیبانی از `tcp`، `tcpmux`، `ws`، `wss`، `wsmux` و `wssmux`
- ساخت خودکار توکن امن
- تولید فرمان آماده نصب سمت Client
- بازکردن اختیاری پورت‌ها در UFW
- دستورات وضعیت، بررسی سلامت، لاگ، راه‌اندازی مجدد و حذف
- به‌روزرسانی امن باینری همراه با پشتیبان‌گیری و Rollback
- تست خودکار Syntax و تولید کانفیگ TOML
