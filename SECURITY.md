# سیاست امنیتی

- هیچ توکن، رمز SSH یا کلید خصوصی را داخل Issue یا مخزن GitHub قرار ندهید.
- فایل‌های `/etc/backhaul/*.toml` شامل توکن هستند و با دسترسی `600` ساخته می‌شوند.
- برای هر کشور یک توکن مستقل استفاده کنید.
- توکن پیش‌فرض وجود ندارد و منوی نمایش کانفیگ، مقدار توکن را مخفی می‌کند.
- پورت کنترل هر تونل را فقط به IP سرور Client محدود کنید، اگر فایروال شما چنین امکانی دارد.
- Cron فقط سرویس خراب و `enabled` را ترمیم می‌کند؛ سرویس سالم یا عمداً `disabled` شده را تغییر نمی‌دهد.
- حذف تونل از منو، ابتدا بکاپ می‌گیرد و فایل‌ها را به آرشیو قابل‌بازیابی منتقل می‌کند.
- گزینه تشخیص شبکه هیچ تغییری در `sysctl.conf` یا `/etc/profile` ایجاد نمی‌کند.
- برای گزارش آسیب‌پذیری، جزئیات حساس را عمومی منتشر نکنید.

این پروژه باینری Backhaul را از Release رسمی پروژه Musixal/Backhaul دریافت می‌کند. پیش
از استفاده در محیط حساس، نسخه و هش فایل دانلودشده را مطابق سیاست امنیتی خود بررسی کنید.
