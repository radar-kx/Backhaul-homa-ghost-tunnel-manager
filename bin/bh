#!/usr/bin/env bash
set -Eeuo pipefail

# Backward-compatible alias. New documentation and installer messages use homa.
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
exec "${BH_HOMA_BIN:-$SCRIPT_DIR/homa}" "$@"
